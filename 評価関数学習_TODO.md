# テトリスAI 評価関数学習 TODO（方針：タイプA）

## 全体方針
まず軽いモデルでパイプラインを完成させ、40 LINE模倣やタイプB（盤面入力）への投資はフェーズ5の実測を見てから判断する。表現力より先に「仕組みを一周させる」ことを優先する。

---

## 段階1：評価関数学習のパイプライン完成（タイプA）

### フェーズ0：設計確定
- [ ] 目的を確認：40 LINE模倣は段階2。段階1は「仕組みを一周させる」ことが目的
- [ ] 既存ソルバー（`placement_solver.py`）が1手全探索か複数手先読みかを確認 ← 40 LINE模倣の鍵になる最重要確認
- [ ] 学習の形を決める：人間が選んだ手を高評価にする選好学習を基本とする

### フェーズ1：データ収集
- [ ] ロック箇所（`hard_drop` / `force_lock`）で「ロック直前の盤面・現在ミノ・NEXT・hold」と「確定配置（rot, col, use_hold）」を記録
- [ ] `use_hold` を正しく記録するためのフラグ管理を実装（`hard_drop` だけでは取れない）
- [ ] 保存形式を決める（JSONL など追記しやすい形）
- [ ] 数ゲーム試し、記録が正しいか検証
- [ ] 収集ペースの目安：30秒100手＝1時間12,000手。まず数時間ぶん集める

### フェーズ2：特徴量候補を用意
- [ ] 既存4特徴量（高さ・穴・凹凸・ライン消去）を土台にする
- [ ] 定番の追加特徴量を調べて候補を10〜20個に増やす（井戸の深さ、最大高さ、列ごとの高さ等）
- [ ] 各合法配置を特徴量ベクトルに変換する処理を実装（既存の評価ロジックを流用）

### フェーズ3：学習・特徴量の寄与を見る
- [ ] 各盤面で合法な全配置を列挙（既存ソルバーの列挙機能を再利用）
- [ ] 「人間が選んだ配置＝正例／選ばなかった合法配置＝負例」の対比データを作る
- [ ] 軽いモデル（線形〜小さい NN）で学習
- [ ] どの特徴量が効いているか（重みの大きさ・寄与）を確認 ← これが「4特徴量以外を見つける」の実質
- [ ] データ量を増やすと一致率が伸びるか確認（頭打ちまで追加）

### フェーズ4：探索への統合
- [ ] 学習した評価関数を、既存ヒューリスティック評価関数と差し替え可能にする（探索の枠組みは維持）
- [ ] 全合法手をこの評価関数でスコアリングし最善手を選ぶ形に接続
- [ ] 無効手が出ないことを確認（合法手だけを評価する設計になっているか）

### フェーズ5：評価・判断
- [ ] 学習版と既存ヒューリスティック版を比較（スコア・ライン数・生存手数）
- [ ] 人間の選択の再現率（一致率）を測定 ← 模倣が目的なので「強さ」だけでなく「人間らしさ」も見る
- [ ] ボトルネックを特定：情報不足か（→タイプB）、計画性不足か（→探索強化）

---

## 段階2：40 LINE模倣への拡張（フェーズ5の後）
- [ ] フェーズ5の結果を見て、投資先を決める
- [ ] 計画性が主因なら：複数手先読みの探索を強化（40 LINE定型に効く公算が高い）
- [ ] 情報が主因なら：タイプB（盤面＋NEXT＋現在ミノ＋hold 入力）を検討。大量データ・重いモデルを覚悟
- [ ] 40 LINE向け特徴量（定型との一致度、テトリスを狙える形か等）を調査・追加
- [ ] 上級者（自分）の 40 LINE プレイを重点的に収集

---

## 注意点
- 依存関係があるのでフェーズ 0 → 1 → 2 … の順は崩さない
- 各フェーズ後に小さく検証を挟む（全部作ってから動かすと切り分けが困難になる）
- 重みは手設定せずデータから学習する（ここが方針Bの核心）


# テトリスAI 評価関数学習 TODO（方針：タイプB／盤面入力）

## 位置づけ
タイプBは、タイプA（特徴量＋重み学習）のフェーズ5で「情報不足がボトルネック」と実測で判明した場合に進む発展方針。いきなり着手しない。生盤面をモデルに入力し、有効な特徴量そのものをモデルに自動構築させる（表現学習）。

## 進む前の判断チェック
- [ ] タイプAのフェーズ5を完了し、一致率が特徴量追加では頭打ちだと確認した
- [ ] ボトルネックが「計画性不足（→探索強化）」ではなく「情報不足」だと切り分けた
- [ ] 代償を了承した：必要データが数十万手規模（数十時間プレイ）に増える／モデルが重い／推論コストが上がる／学習内容が解釈しにくい

---

## フェーズ0：設計確定
- [ ] 入力を決める：盤面（20×10）＋現在ミノ＋NEXT＋hold
- [ ] 盤面の表現を決める（0/1 の 2 値か、ミノ種別を含めるか）
- [ ] モデル構造を決める（盤面の空間構造を扱う CNN 系を基本とする）
- [ ] 出力形式を決める：タイプAと同じく「合法手の集合から選択」を維持し、無効手を構造的に防ぐ
- [ ] 学習の形を決める：人間の選好（選んだ手＞選ばなかった合法手）を基本とする

## フェーズ1：データ収集（タイプAの収集基盤を再利用）
- [ ] タイプAで記録した「盤面・現在ミノ・NEXT・hold・確定配置」をそのまま流用
- [ ] データ量を大幅に増やす（数十万手規模。1時間12,000手なら数十時間ぶん）
- [ ] 上級者（自分）のプレイに絞って収集（模倣元の質が上限を決めるため）
- [ ] データ拡張の可否を検討（左右反転など。テトリスの対称性を利用できるか）

## フェーズ2：前処理・入力構築
- [ ] 盤面をモデル入力形式（テンソル）に変換する処理を実装
- [ ] 現在ミノ・NEXT・hold の埋め込み方法を決める（one-hot 等）
- [ ] 各盤面での合法手列挙を維持（出力を合法手に制限するため。既存ソルバー流用）

## フェーズ3：モデル学習
- [ ] モデルを実装（CNN 系＋合法手スコアリング）
- [ ] 訓練・検証に分割して学習
- [ ] 一致率（人間の選択の再現率）を評価指標にする
- [ ] 過学習・データ不足の兆候を監視
- [ ] 推論速度を計測（全合法手をモデルで評価するため、1手あたりの呼び出し回数×モデル重さを確認）

## フェーズ4：探索への統合
- [ ] 学習したモデルを評価関数として探索に接続（枠組みはタイプAと同じ）
- [ ] 推論コストが実プレイで許容範囲か確認（重い場合は先読み手数やモデルサイズを調整）
- [ ] 無効手が出ないことを確認

## フェーズ5：評価・比較
- [ ] タイプA版・既存ヒューリスティック版・タイプB版を比較
- [ ] 「強さ」と「人間らしさ（一致率）」の両面で評価
- [ ] タイプBが投資（大量データ・重いモデル）に見合う改善を出したか判断
- [ ] 出せていない場合、タイプAに戻る／探索強化に切り替える判断も選択肢に入れる

---

## 注意点
- タイプBはタイプAの上位互換ではない。データ量・計算コスト・解釈性で明確に劣る面がある
- 40 LINE定型の模倣には、入力の厚さより「複数手の計画（探索）」が効く可能性が高い。タイプBに進んでも、計画性の問題は別途探索で対処する必要がある
- 学習内容がブラックボックス化するため、「何を学んだか理解したい」目的とは相性が悪い点に留意